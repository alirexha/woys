# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
build/
dist/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Models / artifacts (no bundled models)
models/
*.onnx
*.pth
*.pt
*.bin
*.safetensors
*.npy
*.wav
*.flac
!tests/fixtures/*.wav

# Sweep capture directories — large, regeneratable, voice-content
# (privacy + size). The summary JSON + PNG next to them is small and
# useful as a record so it stays.
docs/sweep_*/

# Logs / profiling
*.log
*.prof
*.nsys-rep
*.qdrep

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local config
.env
config.local.toml

# Upstream clone (read-only mirror, not part of our source tree)
upstream/

# uv lock cache
.uv-cache/

# the tooling per-machine permission overrides (do NOT track)
./settings.local.json

# v0.9.0 — compiled native helper, built by bin/Makefile
bin/woys-pw-out
